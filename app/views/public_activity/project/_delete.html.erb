<% if activity.trackable %>
  <%= link_to "#{ activity.owner.first_name } #{ activity.owner.last_name }", account_path(activity.trackable.id) %> just deleted a phase <%= activity.trackable.phase_num %> 
  project called <%= link_to "#{ activity.trackable.name }", project_path(activity.trackable.id) %>
<% end %>